name: payment_integration_sample
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  bloc: ^9.0.0
  dartz: ^0.10.1
  dio: ^5.8.0+1
  equatable: ^2.0.7
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_credit_card: ^4.1.0
  flutter_paypal_payment: ^1.0.8
  flutter_stripe: ^11.5.0
  flutter_svg: ^2.0.7
  font_awesome_flutter: ^10.5.0
  flutter_inappwebview: ^6.1.5

dev_dependencies:
  flutter_lints: ^5.0.0
  change_app_package_name: ^1.5.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/images/
